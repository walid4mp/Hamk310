# Security & Production Checklist

- استخدم JWT_SECRET عشوائياً وطويلاً في Render.
- شغّل الـAPI خلف HTTPS فقط في الإنتاج.
- لا تحفظ كلمات المرور الخام؛ MizanPro يستخدم bcrypt.
- احتفظ بنسخ PostgreSQL احتياطية خارج الخدمة عند الحاجة إلى سياسة استمرارية أعمال.
- استخدم Endpoint النسخة الاحتياطية JSON كنسخة تشغيلية مساعدة، وليس بديلاً عن نسخ قاعدة PostgreSQL الكاملة.
- قبل تشغيل الدفع الحقيقي للاشتراكات، اربط مزود دفع موثوق ووثّق webhook للتحقق من حالة الدفع.
- قبل التكامل البنكي، استخدم OAuth/API الرسمي للبنك أو مزود Open Banking المعتمد بدلاً من تخزين كلمات مرور الخدمات البنكية.
